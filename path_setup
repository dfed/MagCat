PATH=/opt/homebrew/bin:$PATH

if which -s rbenv >/dev/null; then
  # Set up Ruby / rbenv
  eval "$(rbenv init - zsh)"
fi

if which -s ruby >/dev/null && which -s gem >/dev/null; then
  PATH="$PATH:$(ruby -r rubygems -e 'puts Gem.user_dir')/bin"
fi
